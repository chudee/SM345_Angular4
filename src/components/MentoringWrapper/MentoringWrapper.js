import React from 'react';

const MentoringWrapper = ({children}) => {
    return (
        <div id="MentoringContainer">
            {children}
        </div>
    );
};

export default MentoringWrapper;